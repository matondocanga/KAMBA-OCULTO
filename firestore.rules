rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permite que qualquer utilizador que tenha feito login 
    // possa ler e escrever dados (criar grupos, enviar mensagens, etc.)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}